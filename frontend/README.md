# Frontend（B）

## Install

```bash
cd frontend
pnpm install
```

## Setup

```bash
cp .env.example .env
```

Fill:

- `VITE_SHOPS_ADDRESS`
- `VITE_ITEMS_ADDRESS`

Optional:

- `VITE_WORKER_URL` for permit fetching (serial / risk).

## Run

```bash
pnpm run dev
```
