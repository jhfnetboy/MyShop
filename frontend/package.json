{
  "name": "@myshop/frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check src/main.js && node --check src/contracts.js && node --check src/config.js",
    "test:e2e": "playwright test",
    "regression": "pnpm check && pnpm build && pnpm test:e2e"
  },
  "dependencies": {
    "viem": "^2.23.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "vite": "^6.1.0"
  }
}
